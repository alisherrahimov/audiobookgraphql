export const sendEmail = async (email: string): Promise<boolean> => {
    try {
        return true;
    } catch (error) {
        return error;
    }
};

export const generateUrl = async (email: string): Promise<boolean> => {
    try {
        return true;
    } catch (error) {
        return error;
    }
}
